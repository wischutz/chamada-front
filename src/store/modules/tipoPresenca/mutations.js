
const mutations = {
    setTiposPresenca (state, tiposPresenca) {
        state.tiposPresenca = tiposPresenca;
    },
    setTipoPresenca (state, tipoPresenca) {
        state.tipoPresenca = tipoPresenca;
    },
};

export default mutations;
  